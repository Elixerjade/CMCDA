aws --version

aws configure

This is the readme file:

Important COmmands to use here is:

terraform init

then to check if any error:

terraform validate

Once things are good then use

terraform plan: This is actually a dry run to check what is going to happen next time..


terraform apply -auto-approve


terraform fmt



terraform destroy -auto-approve